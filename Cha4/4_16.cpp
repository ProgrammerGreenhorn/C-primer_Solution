#include <vector>
int main() {
  int *p;
  std::vector<int> vec;
  int ival;

  if (p != 0 && *p++) {
  }
  if (ival++ && ival) {
  }
  //   if(vec[ival++] <= vec[ival]){

  //   }
  int temp = ++ival;
  if (vec[temp] <= vec[ival]) {
  }
}