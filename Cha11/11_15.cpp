#include <map>
#include <vector>
using std::map;
using std::vector;

int main() {
  map<int, vector<int>>::mapped_type vec;
  map<int, vector<int>>::key_type i;
  map<int, vector<int>>::value_type pa;

  return 0;
}